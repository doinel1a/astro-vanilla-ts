---
export interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<button
  id={id}
  type='button'
  class='bg-accent-primary hover:bg-accent-primary-state focus:bg-accent-primary-state w-16 rounded-xl px-4 py-2 text-xl shadow-md transition-colors'
>
  <slot />
</button>
